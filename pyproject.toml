[build-system]
build-backend = "setuptools.build_meta"
requires = ["setuptools>=61.0"]

[project]
name = "mvgapi"
version = "0.1.0"
authors = [
  {name = "Martin Dziura", email = "m.dziura@tum.de"},
]
description = "An unofficial interface to timetable information of the Münchner Verkehrsgesellschaft (MVG)."
readme = "README.md"
requires-python = ">=3.7"
dependencies = ["aiohttp~=3.8", "furl~=2.1"]
classifiers = [
  "Programming Language :: Python :: 3",
  "License :: OSI Approved :: MIT License",
  "Operating System :: OS Independent",
]

[project.urls]
"Homepage" = "https://github.com/mondbaron/mvgapi"
"Bug Tracker" = "https://github.com/mondbaron/mvgapi/issues"
